{
  "scripts": {
    "start": "teseract serve",
    "lint": "teseract lint",
    "lint:fix": "teseract lint --fix",
    "release": "teseract release",
    "test": "teseract test"
  },
  "devDependencies": {
    "teseract": "^2.5.0"
  }
}
